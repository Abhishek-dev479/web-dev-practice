let articles = document.getElementsByClassName('articles');

// Array.from(articles).forEach((e, i) => {
//     articles[i].addEventListener('click', function(event){
//         console.log(event.target.parentNode);
//         let id = event.target.parentNode.id.slice(-1);
//         console.log(id);
//         fetch(`/${id}`, {method: 'GET'});
//     })
// });

// Array.from(articles).forEach((e, i) => {
//     articles[i]
// })
